'use client';          // ← tell Next.js this component runs in the browser

import { useState } from 'react';

export default function ProfilePage() {
  // …all of your useState, handlers, JSX etc…
}
